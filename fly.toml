app = "speeddash-pro"
kill_signal = "SIGINT"
kill_timeout = 5

[build]
  image = "dockerfile"

[env]
  DISABLE_PAYMENTS = "1"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80
